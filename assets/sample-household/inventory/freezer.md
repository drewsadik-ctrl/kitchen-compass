# Freezer Notes

Use `inventory/items.json` for machine-readable remembered inventory.

Keep this markdown file only for human notes like:
- priority cuts to use soon
- packaging reminders
- context that does not belong in the structured JSON state
